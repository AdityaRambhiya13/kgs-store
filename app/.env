# Quick Shop — Environment Configuration
# Copy this to .env and customize values

# Admin password for the dashboard
ADMIN_PASSWORD=KetanStoreSecure!2026

# Secret key for security (change in production!)
SECRET_KEY=kgs_super_secret_key_8492nc!

# Database file path (Use /data/store.db for Render deployment)
DATABASE_URL=store.db

# Rate limit (requests per minute per IP)
RATE_LIMIT=120
